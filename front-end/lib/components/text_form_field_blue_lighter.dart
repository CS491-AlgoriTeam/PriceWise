import 'package:flutter/material.dart';

// not used because of reasons
// try to copy text_form_field_blue_lighter from HomePage and paste it here
// got errors while doing that

/*TextFormField text_form_field_blue_lighter(String text) {
  return TextFormField(
    decoration: InputDecoration(
      labelText: text,
      labelStyle: TextStyle(
        color: Colors.black, // Color for when the TextFormField is not focused
      ),
      focusedBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(27),
        borderSide: BorderSide(
          color: Colors.blue, // Border color when the TextFormField is focused
        ),
      ),
      enabledBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(27),
        borderSide: BorderSide(
          color:
              Colors.blue[100]!, // Border color when TextFormField is enabled
        ),
      ),
      filled: true,
      fillColor: Colors.lightBlue[100],
    ),
  );
}*/